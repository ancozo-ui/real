# 🏠 AGENT-04: Public Pages

## 역할
당신은 **Public Pages Agent**입니다.  
일반 사용자가 보는 공개 매물 목록, 상세 페이지, 필터링 UI를 구축합니다.

---

## 📥 INPUT
- `src/lib/prisma.ts` — PublicListing 쿼리
- `src/lib/address.ts` — `maskAddress()` (이미 DB에 저장됨, addressMasked 사용)
- `src/lib/area.ts` — `createAreaInfo()`
- `src/lib/types/listing.ts` — `PublicListing`, `ListingFilters`

## 📤 OUTPUT 계약
```
src/app/
  (public)/
    page.tsx               ← 메인 홈 (히어로 + 최신 매물 미리보기)
    listings/
      page.tsx             ← 매물 목록 + 필터
      [id]/page.tsx        ← 매물 상세
  components/
    ListingCard.tsx        ← 매물 카드 컴포넌트
    ListingFilter.tsx      ← 필터 사이드바/모달
    ImageGallery.tsx       ← 이미지 갤러리 (라이트박스)
```

---

## 🛠️ 구현 요구사항

### 홈페이지 (`/`)
- 히어로 섹션: "신뢰할 수 있는 물류창고 임대 정보"
- 최신 공개 매물 6개 카드 그리드
- 필터 빠른 접근 버튼 (지역별, 시설 유형별)
- 뉴스 최신 3개 미리보기

### 매물 목록 (`/listings`)
**필터 기능 (URL 파라미터 기반):**
```
?sido=경기도&facilityType=WAREHOUSE&minArea=100&maxArea=1000&maxRent=500&hasDock=true
```
- 시도/시군구 선택 (계층형)
- 시설 유형 체크박스
- 면적 범위 슬라이더 (㎡)
- 임대료 범위
- 시설 필터 (도크, 사무실, CCTV, 주차)
- 정렬 (최신순, 임대료 낮은순, 면적 큰순)

**매물 카드:**
- 썸네일 (첫 번째 이미지)
- 마스킹된 주소 (addressMasked)
- 시설 유형 배지
- 총면적/임대면적 (㎡, 평 동시 표시)
- 월임대료 (만원)
- 도크/사무실 아이콘
- "문의하기" 버튼

### 매물 상세 (`/listings/[id]`)
- 이미지 갤러리 (라이트박스)
- 마스킹된 주소 (지도 X, 보안 주의)
- 전체 상세 정보 테이블
- 시설 특징 아이콘 그리드
- "문의하기" 버튼 → `/listings/[id]#inquiry`
- 문의 폼 앵커 (AGENT-05가 채움)

### SEO
```typescript
// app/listings/[id]/page.tsx
export async function generateMetadata({ params }) {
  const listing = await getListing(params.id)
  return {
    title: `${listing.addressMasked} | 물류창고 임대`,
    description: `${listing.facilityType} · ${listing.totalAreaSqm}㎡ · 월 ${listing.monthlyRent}만원`,
    openGraph: { images: [listing.images[0]?.url] },
  }
}
```

### DB 쿼리 패턴
```typescript
// 목록 쿼리 (addressFull 제외)
const listings = await prisma.listing.findMany({
  where: {
    status: 'PUBLISHED',
    ...(filters.sido && {
      addressMasked: { startsWith: filters.sido }
    }),
    ...(filters.facilityType && { facilityType: filters.facilityType }),
    ...(filters.minAreaSqm && { totalAreaSqm: { gte: filters.minAreaSqm } }),
    ...(filters.maxRent && { monthlyRent: { lte: filters.maxRent } }),
    ...(filters.hasDock && { hasDock: true }),
  },
  select: {
    id: true,
    addressMasked: true,        // ← addressFull 절대 select 금지
    facilityType: true,
    totalAreaSqm: true,
    totalAreaPyeong: true,
    rentAreaSqm: true,
    rentAreaPyeong: true,
    monthlyRent: true,
    deposit: true,
    hasDock: true,
    hasOffice: true,
    status: true,
    images: {
      select: { url: true, order: true },
      orderBy: { order: 'asc' },
      take: 1,
    },
  },
  orderBy: { createdAt: 'desc' },
  skip: (page - 1) * 20,
  take: 20,
})
```

---

## ✅ 완료 기준
- [ ] `/listings` 페이지 렌더링 (공개 매물 표시)
- [ ] 필터 적용 시 URL 파라미터 반영
- [ ] `/listings/[id]` 상세 페이지 렌더링
- [ ] addressFull이 공개 페이지에 절대 노출되지 않음
- [ ] 이미지 갤러리 작동
- [ ] SEO 메타태그 생성
- [ ] 모바일 반응형

---

## 🔔 완료 신호
```
AGENT-04 완료 보고
✅ 공개 매물 목록/상세: 완료
✅ 필터링: URL 파라미터 기반 작동
✅ addressFull 비노출: 확인
⚠️ AGENT-05에게: /listings/[id]#inquiry 앵커 위치 확인
👉 QA-AGENT 호출 요청
```

---
---
---

# 📬 AGENT-05: Inquiry & Email

## 역할
당신은 **Inquiry & Email Agent**입니다.  
임차 문의 폼, 관리자 이메일 알림, 관리자 문의 목록을 구축합니다.

---

## 📥 INPUT
- `prisma/schema.prisma` — `Inquiry` 모델
- `src/lib/prisma.ts`
- AGENT-04의 `/listings/[id]` 페이지 (문의 폼 삽입 위치)

## 📤 OUTPUT 계약
```
src/
  lib/
    email.ts                 ← sendInquiryNotification(), sendNewsletter()
  app/
    listings/[id]/
      InquiryForm.tsx        ← 클라이언트 컴포넌트
    api/
      inquiry/route.ts       ← 문의 저장 + 이메일 발송
    admin/
      inquiries/
        page.tsx             ← 문의 목록
```

---

## 🛠️ 구현 요구사항

### `src/lib/email.ts`
```typescript
import { Resend } from 'resend'

const resend = new Resend(process.env.RESEND_API_KEY)

export interface InquiryData {
  listingId?: string
  listingAddress?: string
  companyName: string
  contactName: string
  phone: string
  email: string
  message?: string
}

export async function sendInquiryNotification(data: InquiryData): Promise<void> {
  await resend.emails.send({
    from: process.env.RESEND_FROM_EMAIL!,
    to: process.env.ADMIN_NOTIFICATION_EMAIL!,
    subject: `[물류창고 문의] ${data.companyName} - ${data.listingAddress ?? '일반 문의'}`,
    html: `
      <h2>새 임대 문의가 접수되었습니다</h2>
      <table>
        <tr><td>회사명</td><td>${data.companyName}</td></tr>
        <tr><td>담당자</td><td>${data.contactName}</td></tr>
        <tr><td>연락처</td><td>${data.phone}</td></tr>
        <tr><td>이메일</td><td>${data.email}</td></tr>
        <tr><td>관심 매물</td><td>${data.listingAddress ?? '-'}</td></tr>
        <tr><td>메시지</td><td>${data.message ?? '-'}</td></tr>
      </table>
    `,
  })
}

export async function sendNewsletter(
  subject: string,
  htmlContent: string,
  subscribers: { email: string; name?: string }[]
): Promise<void> {
  // Resend Batch API 활용 (AGENT-06이 호출)
  const batchEmails = subscribers.map((sub) => ({
    from: process.env.RESEND_FROM_EMAIL!,
    to: sub.email,
    subject,
    html: htmlContent.replace('{{name}}', sub.name ?? '구독자'),
  }))
  
  // 100개씩 배치 발송
  for (let i = 0; i < batchEmails.length; i += 100) {
    await resend.batch.send(batchEmails.slice(i, i + 100))
  }
}
```

### 문의 폼 (`InquiryForm.tsx`)
필드: 회사명(필수), 담당자명(필수), 연락처(필수), 이메일(필수), 문의내용(선택)  
- 클라이언트 컴포넌트 (useActionState 활용)
- 성공 시 "문의가 접수되었습니다" 메시지
- 전화번호 형식 자동 포맷 (010-0000-0000)
- 스팸 방지: honeypot 필드

### `/api/inquiry/route.ts`
```typescript
// POST: 문의 저장 + 이메일 발송
// Rate limiting: IP당 시간당 5건
// 입력 검증: zod
```

### 관리자 문의 목록 (`/admin/inquiries/page.tsx`)
- 전체 문의 목록 (최신순)
- 매물 연결 정보
- 읽음/안읽음 상태
- 이메일/전화 클릭 가능 링크

---

## ✅ 완료 기준
- [ ] 문의 폼 제출 → DB 저장 확인
- [ ] 제출 시 관리자 이메일 발송 (Resend)
- [ ] 관리자 문의 목록 렌더링
- [ ] 유효성 검증 (필수 항목 누락 시 에러)
- [ ] Rate limiting 작동

---

## 🔔 완료 신호
```
AGENT-05 완료 보고
✅ email.ts: sendInquiryNotification, sendNewsletter 구현
✅ 문의 폼: 저장 + 이메일 작동
⚠️ AGENT-06에게: sendNewsletter(subject, html, subscribers) 시그니처 확인
👉 QA-AGENT 호출 요청
```

---
---
---

# 📰 AGENT-06: News, RSS & Newsletter

## 역할
당신은 **News & Newsletter Agent**입니다.  
블로그 뉴스, RSS 자동 수집, 뉴스레터 발송을 구축합니다.

---

## 📥 INPUT
- `prisma/schema.prisma` — `NewsPost`, `NewsletterSubscriber` 모델
- `src/lib/email.ts` — `sendNewsletter()`
- `src/lib/prisma.ts`

## 📤 OUTPUT 계약
```
src/app/
  news/
    page.tsx             ← 뉴스 목록
    [slug]/page.tsx      ← 뉴스 상세
  api/
    newsletter/
      subscribe/route.ts     ← 구독 신청
      unsubscribe/route.ts   ← 구독 해제
    rss/
      collect/route.ts       ← RSS 수집 (Cron 트리거)
  admin/
    news/
      page.tsx               ← 뉴스 관리
      new/page.tsx           ← 뉴스 작성
      newsletter/page.tsx    ← 뉴스레터 발송
```

---

## 🛠️ 구현 요구사항

### RSS 수집 (`/api/rss/collect/route.ts`)
```typescript
import Parser from 'rss-parser'
import { prisma } from '@/lib/prisma'

const parser = new Parser()

export async function GET(request: Request) {
  // Vercel Cron 또는 수동 트리거
  const authHeader = request.headers.get('authorization')
  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return Response.json({ error: 'Unauthorized' }, { status: 401 })
  }
  
  const feedUrls = (process.env.RSS_FEEDS ?? '').split(',').filter(Boolean)
  let collected = 0
  
  for (const feedUrl of feedUrls) {
    try {
      const feed = await parser.parseURL(feedUrl.trim())
      
      for (const item of feed.items.slice(0, 10)) {
        if (!item.title || !item.link) continue
        
        const slug = generateSlug(item.title)
        
        await prisma.newsPost.upsert({
          where: { slug },
          create: {
            title: item.title,
            slug,
            content: item.content ?? item.contentSnippet ?? '',
            excerpt: item.contentSnippet?.slice(0, 200),
            source: 'RSS',
            sourceUrl: item.link,
            publishedAt: item.pubDate ? new Date(item.pubDate) : new Date(),
          },
          update: {},  // 이미 있으면 스킵
        })
        collected++
      }
    } catch (e) {
      console.error(`RSS 수집 실패: ${feedUrl}`, e)
    }
  }
  
  return Response.json({ collected })
}

function generateSlug(title: string): string {
  return title
    .toLowerCase()
    .replace(/[^\w\s가-힣]/g, '')
    .replace(/\s+/g, '-')
    .slice(0, 100)
    + '-' + Date.now()
}
```

### `vercel.json` (Cron 설정)
```json
{
  "crons": [
    {
      "path": "/api/rss/collect",
      "schedule": "0 9 * * *"
    }
  ]
}
```

### 구독 신청 (`/api/newsletter/subscribe/route.ts`)
- 이메일 중복 체크
- 재구독 처리 (active: false → true)
- 확인 이메일 발송 (선택)

### 관리자 뉴스레터 발송 (`/admin/news/newsletter/page.tsx`)
- 발송 대상: 활성 구독자 목록 + 총 인원 표시
- 제목/내용 입력
- "미리보기" 기능
- "발송" 버튼 → `sendNewsletter()` 호출

---

## ✅ 완료 기준
- [ ] `/api/rss/collect` 호출 시 DB에 뉴스 저장
- [ ] 뉴스 목록/상세 페이지 렌더링
- [ ] 구독 신청 API 작동
- [ ] 관리자 뉴스레터 발송 UI + 실제 발송
- [ ] vercel.json Cron 설정

---

## 🔔 완료 신호
```
AGENT-06 완료 보고
✅ RSS 수집: 구현 완료
✅ 뉴스 목록/상세: 완료
✅ 뉴스레터 발송: 완료
⚠️ AGENT-07에게: 뉴스 카드 UI 통일 요청
👉 QA-AGENT 호출 요청
```
